# WordPress Multisite with Docker (Local Setup)

Този проект представлява професионална инсталация на WordPress Multisite, използвайки **Docker**, **Nginx**, **MariaDB** и **Redis**.
Проектът е конфигуриран да работи на локална виртуална машина (Debian), достъпна през Windows Host машина чрез custom DNS (hosts file).

## 🛠 Технически Стек
*   **OS:** Debian 12 (Virtual Machine)
*   **Web Server:** Nginx (Alpine)
*   **Database:** MariaDB 10.6
*   **CMS:** WordPress (PHP-FPM 8.2)
*   **Cache:** Redis (Object Cache)
*   **Management:** PhpMyAdmin & WP-CLI

## 📂 Структура на проекта
*   `docker-compose.yml` - Основна конфигурация на услугите.
*   `nginx/` - Конфигурационни файлове за Nginx (Multisite rules).
*   `conf/php/` - Custom PHP настройки (Upload limits).
*   `scripts/` - Автоматизирани скриптове за backup.
*   `wp_data/` - Файловете на WordPress (изключени от git).
*   `mysql_data/` - Базата данни (изключена от git).

## 🚀 Бърз старт

1.  **Клониране на репозиторито:**
    ```bash
    git clone https://github.com/TVOETO-IME/repo-name.git
    cd repo-name
    ```

2.  **Настройка на Environment променливи:**
    Създайте `.env` файл (виж `.env.example`) с данните за базата и домейна (`example.com`).

3.  **Стартиране:**
    ```bash
    docker compose up -d
    ```

4.  **Мрежова настройка:**
    За да достъпите сайта локално, трябва да настроите `hosts` файла на вашия компютър. Вижте [docs/network_setup.md](docs/network_setup.md) за детайли.

## ⚙️ Допълнителни функционалности
*   **Redis Cache:** Активиран за оптимизация на заявките към базата.
*   **PhpMyAdmin:** Достъпен на порт `8080` за управление на базата.
*   **Uploads:** Увеличени лимити за качване на файлове до 64MB.
